.DS_Store
*.log
*.swp
*.swo
node_modules/
.idea/
.vscode/
debug.log
# Legacy mockup (kept locally as reference, not versioned)
nuvirahub_full_website_ui.html
# Original theme zip at project root (superseded by dist/nuvirahub.zip)
/nuvirahub.zip
# Sensitive / deploy-only artifacts — never commit
dist/nuvirahub_live.sql
dist/recover-admin.php
dist/nuvirahub/

# Full-site deploy packages (large + contain DB/credentials) — never commit
/dist/live-package/
/dist/nuvirahub-FULL-SITE.zip
/dist/nuvirahub-FULL-SITE/
/dist/nuvirahub-FULL-SITE1/
/dist/nuvirahub-FULL-SITE*/
/260622/

# Production theme zip is a rebuildable build artifact — never commit
/dist/nuvirahub.zip
